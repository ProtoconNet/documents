description: Node information

headers:
  $ref: "../../common/header.yaml"

content:
  application/hal+json:
    schema:
      properties:
        _hint:
          title: _hint
          type: string
          default: "mitum-currency-hal-v0.0.1"

        _embedded:
          type: array
          items:
            $ref: "../schema/embedded.yaml"

        _links:
          type: object

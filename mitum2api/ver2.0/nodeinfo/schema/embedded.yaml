type: object

properties:
  network_id:
    title: network_id
    type: string

  last_manifest:
    title: last_manifest
    $ref: "./last_manifest.yaml"

  network_policy:
    title: network_policy
    $ref: "./network_policy.yaml"

  local:
    title: local
    $ref: "./local.yaml"

  consensus:
    title: consensus
    $ref: "./consensus.yaml"

  _hint:
    title: _hint
    type: string
    default: "node-info-v0.0.1"

required:
  - network_id
  - last_manifest
  - network_policy
  - local
  - consensus

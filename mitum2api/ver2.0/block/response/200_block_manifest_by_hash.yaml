description: Block information

headers:
  $ref: "../../common/header.yaml"

content:
  application/hal+json:
    schema:
      properties:
        _hint:
          title: _hint
          type: string
          default: "mitum-currency-hal-v0.0.1"

        _embedded:
          $ref: "../schema/embedded_block_manifest.yaml"

        _links:
          $ref: "../schema/link_block_manifest.yaml"

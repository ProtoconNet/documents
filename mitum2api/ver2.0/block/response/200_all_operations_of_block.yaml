description: Operations information

headers:
  $ref: "../../common/header.yaml"

content:
  application/hal+json:
    schema:
      type: "object"
      properties:
        _hint:
          type: "string"

        _embedded:
          type: "array"
          items:
            $ref: "../schema/embedded_all_operations_of_block.yaml"

        _links:
          $ref: "../schema/link_all_operations_of_block.yaml"

        _extra:
          $ref: "../schema/extra_all_operations_of_block.yaml"

description: Server error

headers:
  $ref: "./header.yaml"

content:
  application/json:
    schema:
      $ref: "./error.yaml"
    example:
      id: server_error
      message: Unexpected server-side error
